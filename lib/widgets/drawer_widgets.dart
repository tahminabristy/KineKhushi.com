import 'package:flutter/cupertino.dart';

class DrawerNavUser extends StatelessWidget {
  const DrawerNavUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
